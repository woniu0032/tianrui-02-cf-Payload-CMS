name = "tianrui-textile"
compatibility_date = "2024-01-01"

# 构建配置
[build]
command = "pnpm run build"
output_directory = "dist"

# 环境变量
[env.production]
vars = { NODE_ENV = "production" }

[env.staging]
vars = { NODE_ENV = "staging" }
